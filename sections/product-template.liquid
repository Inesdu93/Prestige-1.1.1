<section class="Product Product--{{ section.settings.image_size }}" data-section-id="{{ section.id }}" data-section-type="product" data-section-settings='{{ section_settings }}'>
  <div class="Product__Wrapper">

    <div class="Product__Gallery">
      <div class="Product__Slideshow">
        {% for image in product.images %}
          <div class="Product__SlideItem">
            <img src="{{ image | img_url: '800x' }}" alt="{{ image.alt | escape }}">
          </div>
        {% endfor %}
      </div>
    </div>

    <!-- Pastilles de couleur -->
    <div class="Product__ColorSelector">
      <h4>Couleur</h4>
      <div class="ColorOptions" style="display: flex; gap: 8px; margin-top: 8px;">
        {% for variant in product.variants %}
          {% if variant.option1 contains 'Noir' %}
            <div class="ColorOption" style="background-color: black; width: 20px; height: 20px; border-radius: 50%;"></div>
          {% elsif variant.option1 contains 'Blanc' %}
            <div class="ColorOption" style="background-color: white; border: 1px solid #ddd; width: 20px; height: 20px; border-radius: 50%;"></div>
          {% elsif variant.option1 contains 'Rouge' %}
            <div class="ColorOption" style="background-color: red; width: 20px; height: 20px; border-radius: 50%;"></div>
          {% elsif variant.option1 contains 'Bleu' %}
            <div class="ColorOption" style="background-color: blue; width: 20px; height: 20px; border-radius: 50%;"></div>
          {% else %}
            <div class="ColorOption" style="background-color: gray; width: 20px; height: 20px; border-radius: 50%;"></div>
          {% endif %}
        {% endfor %}
      </div>
    </div>

    <!-- FAQ -->
    <div class="Product__FAQ">
      <h3>FAQ</h3>
      <div class="FAQ__Item">
        <h4>Question 1: Quel est le délai de livraison ?</h4>
        <p>Réponse: Le délai de livraison est de 3 à 5 jours ouvrables.</p>
      </div>
      <div class="FAQ__Item">
        <h4>Question 2: Comment retourner un produit ?</h4>
        <p>Réponse: Vous pouvez retourner un produit dans les 30 jours suivant l'achat.</p>
      </div>
    </div>

  </div>
</section>

</section>
